import { formatDates } from 'elevate-ember/helpers/format-dates';
import { module, test } from 'qunit';

module('Unit | Helper | format dates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDates([42]);
  assert.ok(result);
});
