<div class="center">
  <div class="quote">
    <h2>{{random-quote quote=model}}</h2>

    {{#unless session.isAuthenticated}}
      <button type="submit" class="btn btn-default">{{link-to "Log In" 'login'}}</button>
      <button type="submit" class="btn btn-default">{{link-to 'Sign Up' 'users.new'}}</button>
    {{/unless}}

  </div>
</div>
